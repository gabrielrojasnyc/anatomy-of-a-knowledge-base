services:
  db:
    image: docker.io/pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: kb
      POSTGRES_PASSWORD: kb
      POSTGRES_DB: kb
    ports:
      - "5433:5432"
    volumes:
      - kbdata:/var/lib/postgresql/data
volumes:
  kbdata:
